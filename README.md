# Dataset

This repository have all the table dumps of the database used.
Some files are in gz format but the bigger ones are divided in multipled parts.
Because of that, the directory src has some bash scripts to join the files and to unzip everything.
